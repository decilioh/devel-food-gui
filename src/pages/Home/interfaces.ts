export interface FeedBackProps {
    id: number
    createdAt: string
    comment: string
    rating: number
}

export interface ReviewProps {
    id: number
    imageUrl: string
    idPromotion: string
}